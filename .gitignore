# macOS
.DS_Store

# Design source files (not needed to deploy)
*.psd

# Single-file offline build artifact (generated by build_standalone.py)
Noam Leshem.html

# DC runtime + screenshots (not used by the built site)
support.js
screenshots/

# Local symlink to the asset library (assets are copied into dist/ at build time)
uploads

# Python
__pycache__/
*.pyc
